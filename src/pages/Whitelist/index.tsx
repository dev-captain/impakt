import { Container } from '@chakra-ui/react';

const Whitelist = () => {
  return (
    <Container spacing={0} p={0} m={0} id="whitelist-container" minW="full">
      <section />
      {/* sections come here  */}
    </Container>
  );
};

export default Whitelist;
