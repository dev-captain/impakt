import { HStack } from '@chakra-ui/react';

const RoadMapBackground = () => {
  return <HStack pos="absolute" bottom={0} w="full" spacing={0} width="full" bgColor="glass.700" />;
};

export default RoadMapBackground;
